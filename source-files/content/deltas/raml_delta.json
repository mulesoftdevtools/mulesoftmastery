{"category_slug": "apikit-raml-oas", "subtopics": [{"slug": "raml-data-types", "title": "RAML Data Types In Depth", "meta_description": "Explore RAML 1.0 data types in depth: scalars, objects, arrays, unions, enums and facets, plus how to define reusable types for consistent API contracts.", "reading_time": "8 min read", "summary": "A detailed walkthrough of the RAML 1.0 type system, covering scalar and structured types, facets and reuse patterns.", "content_html": "\n<p>RAML 1.0 introduced a full type system that goes well beyond the JSON Schema references RAML 0.8 relied on. Instead of bolting on an external schema language, RAML lets you describe every shape your API produces or consumes directly in the same file, using a syntax that reads like plain YAML. Getting comfortable with this type system is one of the highest-leverage skills for anyone doing design-first API work on Anypoint Platform, because well-modeled types drive validation, example generation, mocking and even the shape of generated APIkit flows.</p>\n\n<h2>Scalar Types</h2>\n<p>RAML ships with a set of built-in scalar types that map cleanly onto JSON and XML primitives: <code>string</code>, <code>number</code>, <code>integer</code>, <code>boolean</code>, <code>date-only</code>, <code>time-only</code>, <code>datetime-only</code>, <code>datetime</code>, <code>file</code> and <code>nil</code>. The <code>datetime</code> type additionally supports a <code>format</code> facet so you can pin it to <code>rfc3339</code> or <code>rfc2616</code> when your API needs a specific wire format rather than the RAML default.</p>\n\n<h2>Object Types</h2>\n<p>Object types are the workhorse of RAML modeling. You declare <code>properties</code>, mark individual properties as optional with a trailing <code>?</code>, and RAML infers the rest as required. Objects can also declare <code>additionalProperties: false</code> to reject unexpected fields during validation.</p>\n\n<pre><code class=\"language-yaml\">#%RAML 1.0 Library\ntypes:\n  Address:\n    type: object\n    properties:\n      street: string\n      city: string\n      postalCode:\n        type: string\n        pattern: \"^[0-9]{5}$\"\n      country?: string\n\n  Customer:\n    type: object\n    additionalProperties: false\n    properties:\n      id: integer\n      name:\n        type: string\n        minLength: 1\n        maxLength: 80\n      age:\n        type: integer\n        minimum: 0\n        maximum: 130\n      status:\n        enum: [ACTIVE, INACTIVE, SUSPENDED]\n      tags?:\n        type: array\n        items: string\n      billingAddress: Address\n      shippingAddress?: Address\n\n  CustomerOrId:\n    type: Customer | integer</code></pre>\n\n<h2>Array Types</h2>\n<p>Array types are declared either with the shorthand <code>Customer[]</code> or the longhand <code>type: array</code> plus an <code>items</code> facet, which is useful when you also need to constrain <code>minItems</code>, <code>maxItems</code>, or <code>uniqueItems</code>. Both notations are equivalent for simple cases, but the longhand form is required whenever you need to attach facets to the array itself rather than to its members.</p>\n\n<h2>Unions and Enums</h2>\n<p>The pipe operator (<code>|</code>) forms a union type, letting a property accept one of several shapes &mdash; useful for endpoints that return either a full resource or a lightweight reference. Enums restrict a scalar to a fixed set of literal values, which RAML tooling can turn directly into dropdowns or client-side constants.</p>\n\n<h2>Facets</h2>\n<p>Facets are the constraint keywords attached to a type to narrow the values it accepts. Strings support <code>minLength</code>, <code>maxLength</code> and <code>pattern</code> (a regular expression); numbers and integers support <code>minimum</code>, <code>maximum</code>, <code>format</code> and <code>multipleOf</code>; arrays support <code>minItems</code>, <code>maxItems</code> and <code>uniqueItems</code>. These facets are enforced automatically by APIkit's inbound validation, so a malformed payload is rejected with a 400 before your flow logic ever runs.</p>\n\n<h2>Reusing Types</h2>\n<p>Every type you define under the top-level <code>types:</code> section (or inside a RAML library) becomes referenceable by name anywhere else in the document, including inside other type definitions, as request/response bodies, and as query parameter types. This is what makes RAML modeling compositional: define <code>Address</code> once, then reuse it inside <code>Customer</code>, <code>Order</code> and <code>Shipment</code> without duplicating the field list.</p>\n\n<table>\n<tr><th>Category</th><th>Examples</th><th>Typical Facets</th></tr>\n<tr><td>Scalar</td><td>string, number, integer, boolean, datetime</td><td>pattern, minimum, maximum, format</td></tr>\n<tr><td>Object</td><td>Customer, Address, Order</td><td>properties, additionalProperties</td></tr>\n<tr><td>Array</td><td>Customer[], array of string</td><td>minItems, maxItems, uniqueItems</td></tr>\n<tr><td>Union</td><td>Customer | integer</td><td>(inherits facets of each member)</td></tr>\n</table>\n\n<h2>Key Points</h2>\n<ul>\n  <li>RAML's built-in scalars cover the same ground as JSON primitives plus dedicated date/time variants.</li>\n  <li>Object types use <code>properties</code>, optional <code>?</code> suffixes, and <code>additionalProperties: false</code> for strict validation.</li>\n  <li>Arrays can be declared with the <code>Type[]</code> shorthand or the longhand form when facets like <code>minItems</code> are needed.</li>\n  <li>Union types (<code>|</code>) and enums model \"one of several\" values without external schemas.</li>\n  <li>Facets such as <code>pattern</code>, <code>minLength</code> and <code>minimum</code>/<code>maximum</code> give you declarative validation enforced automatically by APIkit.</li>\n  <li>Defining types once under <code>types:</code> and referencing them everywhere keeps large specs consistent and DRY.</li>\n</ul>\n"}, {"slug": "raml-traits", "title": "RAML Traits", "meta_description": "Learn how RAML traits let you reuse method-level behavior like pagination, auth headers and rate limiting across resources, including parameterized traits.", "reading_time": "7 min read", "summary": "An explanation of RAML traits, the reusable method-level building blocks used to avoid repeating common request and response behavior.", "content_html": "\n<p>Real APIs tend to repeat the same handful of behaviors across dozens of endpoints: pagination query parameters, rate-limit response headers, standard error responses on a secured method. Copy-pasting these details into every method definition is exactly the kind of duplication RAML was designed to eliminate, and the mechanism it provides for method-level reuse is called a <strong>trait</strong>.</p>\n\n<h2>What a Trait Is</h2>\n<p>A trait is a partial method definition &mdash; a fragment containing any of the things a method can declare, such as <code>queryParameters</code>, <code>headers</code>, <code>responses</code> or a <code>description</code>. Traits do not stand alone; they are mixed into a concrete <code>get</code>, <code>post</code>, <code>put</code>, <code>delete</code> or <code>patch</code> definition using the <code>is:</code> keyword, either at the method level or at the resource level (where it applies to every method on that resource).</p>\n\n<pre><code class=\"language-yaml\">#%RAML 1.0\ntitle: Catalog API\nversion: v1\nbaseUri: https://api.example.com/{version}\n\ntraits:\n  secured:\n    description: Requires a valid bearer token\n    headers:\n      Authorization:\n        description: Bearer token\n        type: string\n    responses:\n      401:\n        body:\n          application/json:\n            example: |\n              { \"error\": \"Unauthorized\" }\n\n  paginated:\n    queryParameters:\n      offset:\n        type: integer\n        default: 0\n        minimum: 0\n      limit:\n        type: integer\n        default: 20\n        maximum: 100\n\n  rateLimited:\n    description: Limited to <<maxCalls>> calls per <<period>>\n    responses:\n      429:\n        description: Too many requests, limited to <<maxCalls>> per <<period>>\n\n/products:\n  get:\n    is: [ secured, paginated, rateLimited: { maxCalls: 100, period: \"minute\" } ]\n    responses:\n      200:\n        body:\n          application/json:\n            type: Product[]</code></pre>\n\n<h2>Parameterized Traits</h2>\n<p>Traits become dramatically more useful once you parameterize them. Any token written as <code>&lt;&lt;tokenName&gt;&gt;</code> inside a trait is a placeholder that gets substituted when the trait is applied. In the <code>rateLimited</code> trait above, <code>&lt;&lt;maxCalls&gt;&gt;</code> and <code>&lt;&lt;period&gt;&gt;</code> are filled in at the point of use, so the same trait definition can describe a 100-per-minute limit on one endpoint and a 10-per-second limit on another without duplicating the trait itself.</p>\n\n<h2>Applying Multiple Traits</h2>\n<p>The <code>is:</code> array can list any number of traits, and RAML merges their contents into the method definition. Order in the list does not imply precedence for most fields, but when two traits declare the same response code, tooling behavior can vary, so it is best practice to keep traits focused on non-overlapping concerns (one trait for auth, one for paging, one for rate limiting) rather than building a single monolithic trait.</p>\n\n<h2>Resource-Level Application</h2>\n<p>Traits are not limited to individual methods. Declaring <code>is: [secured]</code> directly on a resource applies that trait to every method nested underneath it, which is a convenient shortcut for resources where every operation requires the same header or response.</p>\n\n<h2>Traits vs. Resource Types</h2>\n<p>Traits and resource types both exist to eliminate duplication, but they operate at different levels of granularity. A trait augments an existing method with extra headers, parameters or responses; it never defines a method on its own. A resource type, by contrast, can define whole methods from scratch, including bodies. In practice, a well-organized RAML spec uses resource types to establish the overall shape of an endpoint (list, create, retrieve) and layers traits on top for cross-cutting concerns like authentication or pagination that apply across many otherwise-different resources.</p>\n\n<table>\n<tr><th>Trait</th><th>Typical Purpose</th><th>Common Parameters</th></tr>\n<tr><td>secured</td><td>Adds an Authorization header and 401 response</td><td>none, or a scheme name</td></tr>\n<tr><td>paginated</td><td>Adds offset/limit query parameters</td><td>defaultLimit, maxLimit</td></tr>\n<tr><td>rateLimited</td><td>Documents throttling behavior and a 429 response</td><td>maxCalls, period</td></tr>\n</table>\n\n<h2>Key Points</h2>\n<ul>\n  <li>A trait is a reusable, partial method definition mixed in with the <code>is:</code> keyword.</li>\n  <li>Traits can be applied to a single method or to an entire resource, cascading to all its methods.</li>\n  <li>Parameterized traits use <code>&lt;&lt;tokenName&gt;&gt;</code> placeholders that are filled in at each application site.</li>\n  <li>Keeping each trait focused on one concern (auth, paging, rate limiting) avoids conflicting merges.</li>\n  <li>Traits reduce duplication without generating extra resources, unlike resource types which model whole endpoints.</li>\n</ul>\n"}, {"slug": "raml-resource-types", "title": "RAML Resource Types", "meta_description": "Understand RAML resource types for reusing entire endpoint patterns such as CRUD collections and members, with fully parameterized, real-world examples.", "reading_time": "7 min read", "summary": "A practical guide to RAML resource types, the mechanism for reusing full resource-level patterns like collection and member endpoints.", "content_html": "\n<p>Where traits reuse a slice of a method's behavior, <strong>resource types</strong> reuse an entire resource's shape &mdash; the full set of methods, their bodies and their responses. If your API has ten resources that all follow the same \"list, create, retrieve, update, delete\" CRUD pattern, a resource type lets you write that pattern once and apply it ten times, dramatically shrinking the spec and keeping every resource consistent.</p>\n\n<h2>Defining a Resource Type</h2>\n<p>A resource type is declared under the top-level <code>resourceTypes:</code> section. Inside it you write method stubs (<code>get</code>, <code>post</code>, and so on) exactly as you would on a real resource, including bodies, responses and query parameters. Two special shorthand names, <code>get</code> and <code>post</code>, can appear without a colon-body when you only need to mark the method as present without extra detail, but most real resource types spell out full bodies and responses.</p>\n\n<pre><code class=\"language-yaml\">#%RAML 1.0\ntitle: Inventory API\nversion: v1\nbaseUri: https://api.example.com/{version}\n\ntypes:\n  Product:\n    type: object\n    properties:\n      id: integer\n      name: string\n      price: number\n\nresourceTypes:\n  collection:\n    get:\n      description: List all <<resourcePathName>>\n      responses:\n        200:\n          body:\n            application/json:\n              type: <<typeName>>[]\n    post:\n      description: Create a new <<resourcePathName|!singularize>>\n      body:\n        application/json:\n          type: <<typeName>>\n      responses:\n        201:\n          body:\n            application/json:\n              type: <<typeName>>\n\n  member:\n    get:\n      description: Retrieve a single <<resourcePathName|!singularize>>\n      responses:\n        200:\n          body:\n            application/json:\n              type: <<typeName>>\n        404:\n          description: Not found\n    delete:\n      description: Remove a <<resourcePathName|!singularize>>\n      responses:\n        204:\n\n/products:\n  type: { collection: { typeName: Product } }\n  /{productId}:\n    type: { member: { typeName: Product } }</code></pre>\n\n<h2>Parameters and Built-in Transformers</h2>\n<p>Like traits, resource types support <code>&lt;&lt;tokenName&gt;&gt;</code> parameters, but they also get two automatic tokens for free: <code>&lt;&lt;resourcePathName&gt;&gt;</code> (the last segment of the resource's URI) and <code>&lt;&lt;resourcePath&gt;&gt;</code> (the full path). RAML also ships built-in string transformers you can pipe a token through, such as <code>!singularize</code>, <code>!pluralize</code>, <code>!uppercase</code> and <code>!lowercamelcase</code>, which is how the example above turns <code>products</code> into \"product\" for singular descriptions.</p>\n\n<h2>Resource Types vs. Traits</h2>\n<p>It is easy to conflate the two, so it helps to think of resource types as answering \"what does this whole endpoint look like\" and traits as answering \"what extra behavior does this one method need.\" They compose together: a resource can have both a <code>type</code> and an <code>is</code> list at the same time.</p>\n\n<h2>Overriding Inherited Behavior</h2>\n<p>A resource that applies a type is free to add its own methods or override individual facets of an inherited method &mdash; declaring a <code>description</code> or additional <code>responses</code> directly on the resource layers on top of, rather than replaces, what the resource type contributed. This lets you keep a shared pattern for the common case while still accommodating the occasional resource that needs an extra response code or a stricter body type.</p>\n\n<table>\n<tr><th>Aspect</th><th>Resource Type</th><th>Trait</th></tr>\n<tr><td>Scope</td><td>Whole resource (multiple methods)</td><td>Single method (or all methods on a resource)</td></tr>\n<tr><td>Keyword</td><td><code>type:</code></td><td><code>is:</code></td></tr>\n<tr><td>Typical use</td><td>CRUD patterns, collection/member shapes</td><td>Pagination, auth headers, rate limits</td></tr>\n<tr><td>Can combine with the other?</td><td>Yes</td><td>Yes</td></tr>\n</table>\n\n<h2>Key Points</h2>\n<ul>\n  <li>Resource types reuse an entire resource's method set, bodies and responses, declared under <code>resourceTypes:</code>.</li>\n  <li>Apply one with <code>type: typeName</code> or <code>type: { typeName: { param: value } }</code> for parameterized versions.</li>\n  <li>Built-in tokens like <code>&lt;&lt;resourcePathName&gt;&gt;</code> plus transformers like <code>!singularize</code> reduce boilerplate further.</li>\n  <li>Resource types and traits solve different problems and are commonly combined on the same resource.</li>\n  <li>Standardizing CRUD resources this way keeps large APIs consistent as they grow.</li>\n</ul>\n"}, {"slug": "raml-security-schemes", "title": "RAML Security Schemes", "meta_description": "Learn how to define OAuth 2.0, Basic Authentication and API Key security schemes in RAML 1.0, then apply them to resources and methods with securedBy.", "reading_time": "7 min read", "summary": "A walkthrough of declaring reusable security schemes in RAML and applying them across an API with the securedBy keyword.", "content_html": "\n<p>Documenting an API's authentication requirements as prose comments is easy to get wrong and easy for consumers to miss. RAML instead treats security as a first-class, structured concept: you declare named <strong>security schemes</strong> once, describing exactly what headers, query parameters or OAuth flows they require, and then attach those schemes to resources or methods with a single keyword.</p>\n\n<h2>The securitySchemes Section</h2>\n<p>Security schemes live under a top-level <code>securitySchemes:</code> section. Each scheme has a <code>type</code> &mdash; commonly <code>OAuth 2.0</code>, <code>Basic Authentication</code>, <code>Digest Authentication</code>, <code>Pass Through</code> (the closest fit for a simple API key) or <code>x-custom</code> for anything else &mdash; plus a <code>describedBy</code> block that documents the exact headers, query parameters and responses associated with using that scheme.</p>\n\n<pre><code class=\"language-yaml\">#%RAML 1.0\ntitle: Payments API\nversion: v1\nbaseUri: https://api.example.com/{version}\n\nsecuritySchemes:\n  oauth2:\n    type: OAuth 2.0\n    describedBy:\n      headers:\n        Authorization:\n          description: Bearer access token\n          type: string\n      responses:\n        401:\n          description: Missing or invalid token\n        403:\n          description: Insufficient scope\n    settings:\n      authorizationUri: https://auth.example.com/oauth2/authorize\n      accessTokenUri: https://auth.example.com/oauth2/token\n      authorizationGrants: [authorization_code, client_credentials]\n      scopes: [\"payments:read\", \"payments:write\"]\n\n  apiKey:\n    type: Pass Through\n    describedBy:\n      headers:\n        X-Api-Key:\n          description: API key issued in the developer portal\n          type: string\n      responses:\n        401:\n          description: Missing or invalid API key\n\n  basicAuth:\n    type: Basic Authentication\n    describedBy:\n      responses:\n        401:\n          description: Invalid username or password\n\nsecuredBy: [apiKey]\n\n/payments:\n  post:\n    securedBy: [oauth2]\n    body:\n      application/json:\n        type: object\n    responses:\n      201:\n</code></pre>\n\n<h2>Applying Schemes with securedBy</h2>\n<p>The <code>securedBy:</code> keyword accepts a list, and it can appear at the root of the document (applying a default to every resource), on a specific resource, or on an individual method, with the more specific declaration overriding the root default. Passing <code>null</code> as one of the list entries marks that scope as explicitly unsecured, which is useful for a public health-check endpoint on an otherwise secured API.</p>\n\n<h2>OAuth 2.0 Settings</h2>\n<p>For the <code>OAuth 2.0</code> type, the <code>settings</code> block captures the details RAML understands natively: <code>authorizationUri</code>, <code>accessTokenUri</code>, the allowed <code>authorizationGrants</code> (authorization_code, client_credentials, password, implicit), and a <code>scopes</code> list. Anypoint API Manager and Design Center can read these settings to pre-populate policy configuration and interactive documentation.</p>\n\n<h2>Choosing a Scheme Type</h2>\n<p>RAML does not have a dedicated \"API Key\" type; the community convention is to model a header- or query-parameter-based key using <code>Pass Through</code>, since that type exists specifically for schemes RAML does not need to validate the mechanics of, only document.</p>\n\n<h2>Security Schemes and API Manager Policies</h2>\n<p>Declaring a security scheme in RAML is purely descriptive &mdash; it documents the contract but does not enforce anything at runtime by itself. Actual enforcement in a deployed Mule application comes from an API Manager policy (such as an OAuth 2.0 Access Token Enforcement policy, a Basic Authentication policy, or a Client ID Enforcement policy) applied to the proxy or the API implementation. Keeping the RAML <code>securitySchemes</code> block aligned with the policies actually configured in API Manager avoids a common source of confusion where the published contract promises one authentication mechanism while the runtime enforces another.</p>\n\n<table>\n<tr><th>Scheme Type</th><th>Typical Use</th><th>Key Settings</th></tr>\n<tr><td>OAuth 2.0</td><td>Delegated auth with scopes</td><td>authorizationUri, accessTokenUri, scopes</td></tr>\n<tr><td>Basic Authentication</td><td>Username/password over HTTPS</td><td>(none required)</td></tr>\n<tr><td>Pass Through</td><td>API keys, custom headers</td><td>describedBy headers/queryParameters</td></tr>\n<tr><td>x-custom</td><td>Anything not otherwise modeled</td><td>Fully custom describedBy</td></tr>\n</table>\n\n<h2>Key Points</h2>\n<ul>\n  <li>Security schemes are declared once under <code>securitySchemes:</code> and reused via <code>securedBy:</code>.</li>\n  <li><code>securedBy</code> can be set at root, resource, or method level, with more specific scopes overriding broader ones.</li>\n  <li>OAuth 2.0 schemes carry a <code>settings</code> block with authorization/token URIs, grants and scopes.</li>\n  <li>API keys are typically modeled with the <code>Pass Through</code> type plus a header or query parameter in <code>describedBy</code>.</li>\n  <li>Using <code>null</code> in a <code>securedBy</code> list explicitly marks an endpoint as unsecured.</li>\n</ul>\n"}, {"slug": "raml-libraries-and-includes", "title": "RAML Libraries, Includes and Overlays", "meta_description": "See how RAML's !include directive, uses libraries, overlays and extensions let large API specs be split across files and adapted for different variants.", "reading_time": "7 min read", "summary": "An overview of the file-organization tools RAML provides, from raw includes to full libraries, overlays and extensions.", "content_html": "\n<p>A single RAML file describing a large API quickly becomes unwieldy: hundreds of lines of types, dozens of resources, and multiple security schemes all in one document. RAML provides three complementary mechanisms for splitting specifications across files and adapting a base spec for different needs: the raw <code>!include</code> tag, structured <strong>libraries</strong>, and <strong>overlays</strong>/<strong>extensions</strong>.</p>\n\n<h2>The !include Directive</h2>\n<p>The simplest tool is <code>!include</code>, a YAML custom tag that pulls the contents of another file in verbatim at that point in the document. It works for whole fragments (a types file, an examples file, even a whole resource) and is commonly used to keep example payloads out of the main spec file.</p>\n\n<pre><code class=\"language-yaml\">#%RAML 1.0\ntitle: Orders API\nversion: v1\nbaseUri: https://api.example.com/{version}\n\ntypes:\n  Order: !include types/order.raml\n\n/orders:\n  post:\n    body:\n      application/json:\n        type: Order\n        example: !include examples/order-example.json\n    responses:\n      201:\n        body:\n          application/json:\n            type: Order</code></pre>\n\n<h2>Libraries with uses:</h2>\n<p>A RAML <strong>library</strong> is a dedicated file (conventionally starting with <code>#%RAML 1.0 Library</code>) that bundles related types, traits, resource types and security schemes for reuse across multiple API specs, not just multiple resources within one spec. A library is imported with the <code>uses:</code> keyword, bound to a namespace, and its contents are then referenced with a dotted prefix.</p>\n\n<pre><code class=\"language-yaml\">#%RAML 1.0 Library\nusage: Common types shared across the Commerce APIs\ntypes:\n  Money:\n    type: object\n    properties:\n      amount: number\n      currency: string\n\ntraits:\n  paginated:\n    queryParameters:\n      offset: integer\n      limit: integer</code></pre>\n\n<pre><code class=\"language-yaml\">#%RAML 1.0\ntitle: Orders API\nversion: v1\nbaseUri: https://api.example.com/{version}\nuses:\n  common: libraries/common.raml\n\ntypes:\n  Order:\n    type: object\n    properties:\n      id: integer\n      total: common.Money\n\n/orders:\n  get:\n    is: [ common.paginated ]\n    responses:\n      200:\n</code></pre>\n\n<h2>Overlays and Extensions</h2>\n<p>Overlays and extensions both start from an existing, complete RAML document and layer changes on top of it without editing the original file. An <strong>overlay</strong> is restricted to non-structural changes &mdash; documentation, descriptions, examples, annotations &mdash; and cannot add or remove resources, methods or types. An <strong>extension</strong> is more powerful and can add new resources, methods, security schemes or types, making it suitable for describing an environment-specific or partner-specific variant of a base API. Both use <code>extends:</code> pointing at the master file and only need to include the fragments that differ.</p>\n\n<pre><code class=\"language-yaml\">#%RAML 1.0 Overlay\nextends: orders-api.raml\ntitle: Orders API (Spanish Documentation)\n/orders:\n  get:\n    description: Recupera una lista de pedidos</code></pre>\n\n<h2>Choosing the Right Tool</h2>\n<p>Use <code>!include</code> for simple file splitting within one spec, a library when the same types/traits/schemes need to be shared across several independent API specs, an overlay for purely descriptive variants (translations, extra documentation), and an extension when a partner or environment genuinely needs extra resources or fields on top of the base contract.</p>\n\n<table>\n<tr><th>Mechanism</th><th>Can Add Structure?</th><th>Typical Use</th></tr>\n<tr><td>!include</td><td>N/A (raw file inclusion)</td><td>Splitting one spec into multiple files</td></tr>\n<tr><td>Library (uses:)</td><td>N/A (defines reusable building blocks)</td><td>Sharing types/traits across many APIs</td></tr>\n<tr><td>Overlay</td><td>No</td><td>Translations, extra docs, examples</td></tr>\n<tr><td>Extension</td><td>Yes</td><td>Partner- or environment-specific variants</td></tr>\n</table>\n\n<h2>Key Points</h2>\n<ul>\n  <li><code>!include</code> pulls another file's raw contents in at a given point, ideal for keeping large specs manageable.</li>\n  <li>RAML libraries bundle types, traits, resource types and security schemes for reuse across multiple API projects via <code>uses:</code>.</li>\n  <li>Overlays add documentation-only changes on top of a base spec; they cannot alter structure.</li>\n  <li>Extensions can add new resources, methods or types on top of a base spec for environment- or partner-specific variants.</li>\n  <li>Both overlays and extensions reference the base file with <code>extends:</code> and only need to declare what changes.</li>\n</ul>\n"}, {"slug": "raml-examples-and-annotations", "title": "RAML Examples and Annotations", "meta_description": "Understand how RAML's example and examples keywords power mocking and Design Center tooling, and how custom annotations extend a spec with extra metadata.", "reading_time": "6 min read", "summary": "A closer look at how sample payloads and custom annotations make a RAML specification more useful for tooling and API consumers.", "content_html": "\n<p>A type definition tells consumers the shape of a payload, but it does not tell them what a realistic payload actually looks like. RAML closes that gap with the <code>example</code> and <code>examples</code> keywords, and it lets teams attach arbitrary structured metadata to any part of a spec through <strong>annotations</strong>. Both features matter more than they might first appear, because Anypoint's Mocking Service, API Console and Design Center all read directly from them.</p>\n\n<h2>Single Examples</h2>\n<p>The singular <code>example</code> keyword attaches one sample value to a type or a body. It can be inline, or pulled from a file with <code>!include</code>, which keeps large JSON or XML samples out of the main spec.</p>\n\n<pre><code class=\"language-yaml\">types:\n  Product:\n    type: object\n    properties:\n      id: integer\n      name: string\n      price: number\n    example:\n      id: 1024\n      name: Wireless Mouse\n      price: 24.99\n\n/products/{productId}:\n  get:\n    responses:\n      200:\n        body:\n          application/json:\n            type: Product\n            example: !include examples/product-example.json</code></pre>\n\n<h2>Multiple Named Examples</h2>\n<p>When one payload shape needs several representative variations &mdash; a minimal order versus a fully discounted order, for instance &mdash; the plural <code>examples</code> keyword accepts a map of named samples instead of a single value. Anypoint's Mocking Service can then be asked to return a specific named example by request header, which is invaluable for testing edge cases against a mock before any backend exists.</p>\n\n<pre><code class=\"language-yaml\">    responses:\n      200:\n        body:\n          application/json:\n            type: Order\n            examples:\n              minimal:\n                id: 1\n                items: []\n                total: 0\n              discounted:\n                id: 2\n                items: [ { sku: \"ABC\", qty: 3 } ]\n                total: 45.00\n                discountCode: \"SUMMER10\"</code></pre>\n\n<h2>Why Examples Matter</h2>\n<p>RAML can auto-generate an example from a type definition if none is supplied, but a hand-written example communicates intent that a schema alone cannot: realistic identifiers, believable string lengths, sensible enum choices. This is what makes the Mocking Service and Design Center's \"Try It\" console produce responses that look like real data rather than <code>string</code> or <code>0</code> placeholders, and it gives frontend teams something concrete to build against before the backend flow exists.</p>\n\n<h2>Custom Annotations</h2>\n<p>Annotations let you attach arbitrary, structured metadata anywhere in a RAML document &mdash; on a resource, a method, a type, even a property &mdash; using parenthesized syntax: <code>(annotationName)</code>. Before an annotation can be used it must be declared under <code>annotationTypes:</code>, which defines the shape the metadata must take, exactly like defining a regular type.</p>\n\n<pre><code class=\"language-yaml\">annotationTypes:\n  deprecated:\n    type: object\n    properties:\n      since: string\n      replacement: string\n  audience:\n    type: string\n    enum: [internal, partner, public]\n\n/legacy-orders:\n  (deprecated):\n    since: \"v2\"\n    replacement: \"/orders\"\n  (audience): internal\n  get:\n    description: Deprecated in favor of /orders\n    responses:\n      200:</code></pre>\n\n<p>Common uses include marking deprecated endpoints, tagging resources for internal governance dashboards, recording the owning team, or feeding custom code-generation templates with information RAML's built-in vocabulary has no keyword for.</p>\n\n<table>\n<tr><th>Feature</th><th>Keyword</th><th>Primary Consumer</th></tr>\n<tr><td>Single sample payload</td><td><code>example</code></td><td>API Console, Mocking Service</td></tr>\n<tr><td>Multiple named samples</td><td><code>examples</code></td><td>Mocking Service (select by header)</td></tr>\n<tr><td>Custom metadata</td><td><code>(annotationName)</code> + <code>annotationTypes</code></td><td>Governance tooling, code generators</td></tr>\n</table>\n\n<h2>Key Points</h2>\n<ul>\n  <li><code>example</code> attaches one sample payload; <code>examples</code> attaches several named variants.</li>\n  <li>Examples can be inlined or pulled from external files with <code>!include</code> to keep specs readable.</li>\n  <li>Hand-written examples produce far more realistic mock responses than auto-generated ones.</li>\n  <li>Annotations use <code>(name)</code> syntax and must first be declared under <code>annotationTypes:</code>.</li>\n  <li>Annotations are a clean extension point for metadata RAML has no built-in keyword for, such as deprecation notices or ownership tags.</li>\n</ul>\n"}]}