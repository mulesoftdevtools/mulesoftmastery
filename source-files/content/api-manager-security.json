{
  "slug": "api-manager-security",
  "title": "API Manager & Security",
  "subtopics": [
    {
      "slug": "api-manager-overview",
      "title": "API Manager Overview",
      "meta_description": "Understand what Anypoint API Manager does: managing API instances, applying policies, tracking analytics, and governing the API lifecycle.",
      "reading_time": "6 min read",
      "summary": "API Manager is the Anypoint Platform component used to govern, secure, and monitor APIs after they have been implemented and deployed.",
      "content_html": "\n<p>Building a well-designed API and deploying it is only part of the job. Once real clients start calling it, you need a way to enforce security policies, control who can access it, throttle traffic, and see how it is being used &mdash; without touching the implementation code every time a rule changes. That is exactly what Anypoint API Manager is for.</p>\n\n<h2>What API Manager Does</h2>\n<p>API Manager is the governance layer of Anypoint Platform. It lets you register an API, create <strong>API instances</strong> tied to specific environments, attach reusable <strong>policies</strong>, define <strong>SLA tiers</strong> for different consumer tiers, and view analytics on traffic and errors &mdash; all without redeploying the underlying Mule application.</p>\n\n<h2>API Instances</h2>\n<p>When you register an API in API Manager, you create an <strong>API instance</strong>: a managed representation of a specific version of your API running in a specific environment (e.g., Sandbox or Production). Each instance is what policies, SLA tiers, and contracts attach to. A single logical API (say, the Orders API) will typically have multiple instances across environments as it moves through its lifecycle.</p>\n\n<h2>Managed vs Unmanaged APIs</h2>\n<ul>\n<li><strong>Managed (embedded) APIs</strong> &mdash; the API implementation includes the Anypoint Runtime autodiscovery configuration, so policies enforced in API Manager are applied directly inside the running Mule application at the API's entry point.</li>\n<li><strong>Managed (via API gateway proxy)</strong> &mdash; a lightweight proxy application sits in front of the backend implementation and enforces policies, useful when you cannot modify the backend directly (e.g., a legacy or third-party service).</li>\n</ul>\n\n<h2>Enabling Autodiscovery</h2>\n<p>To connect a Mule application to its API Manager instance so policies actually take effect, you configure <strong>API Autodiscovery</strong> in the application, pointing it at the API ID from API Manager.</p>\n<pre><code class=\"language-xml\">&lt;api-gateway:autodiscovery apiId=\"12345678\" flowRef=\"orders-api-main\" /&gt;</code></pre>\n<p>Once this is in place, any policy you apply in API Manager for that instance is enforced at runtime, without changing the flow logic itself.</p>\n\n<h2>Core Capabilities of API Manager</h2>\n<table>\n<thead><tr><th>Capability</th><th>Purpose</th></tr></thead>\n<tbody>\n<tr><td>Policies</td><td>Enforce security, traffic control, and transformation rules (e.g., OAuth 2.0, rate limiting, client ID enforcement)</td></tr>\n<tr><td>Contracts</td><td>Approve or reject requests from client applications to consume an API under a given SLA tier</td></tr>\n<tr><td>SLA tiers</td><td>Define different levels of access (e.g., Gold, Silver, Bronze) with different rate limits</td></tr>\n<tr><td>Analytics</td><td>Dashboards on request volume, response times, and error rates per API</td></tr>\n<tr><td>Alerts</td><td>Notify owners when thresholds (error rate, latency, traffic spikes) are crossed</td></tr>\n</tbody>\n</table>\n\n<h2>Typical Governance Workflow</h2>\n<ol>\n<li>Developer publishes the API specification (RAML/OAS) to Anypoint Exchange.</li>\n<li>API is registered as an instance in API Manager, tied to an environment.</li>\n<li>Platform team applies policies (e.g., Client ID Enforcement, rate limiting) appropriate for that environment.</li>\n<li>Consuming teams discover the API in Exchange and request access, creating a contract against an SLA tier.</li>\n<li>API owner approves or auto-approves the contract, issuing client credentials.</li>\n<li>Traffic flows, and the API owner monitors usage through Analytics and Alerts.</li>\n</ol>\n\n<blockquote>Tip: Policies applied in API Manager take effect without a code change or redeploy &mdash; this separation between implementation and governance is one of the biggest productivity wins of the platform.</blockquote>\n\n<h2>Key Points</h2>\n<ul>\n<li>API Manager governs APIs after deployment: policies, contracts, SLA tiers, and analytics.</li>\n<li>An API instance represents a specific version of an API in a specific environment.</li>\n<li>Autodiscovery connects a running Mule application to its API Manager instance so policies are enforced at runtime.</li>\n<li>Governance is decoupled from implementation, so security and traffic rules can change without redeploying code.</li>\n</ul>\n"
    },
    {
      "slug": "api-policy-types-overview",
      "title": "Types of API Policies in Anypoint Platform",
      "meta_description": "A complete tour of the major out-of-the-box Anypoint Platform policy categories, what each one does in practice, and when to reach for it in API Manager.",
      "reading_time": "6 min read",
      "summary": "Anypoint Platform ships a broad catalog of pluggable policies that cover traffic control, identity, and payload security without touching application code.",
      "content_html": "\n<p>One of the biggest advantages of putting an API behind Anypoint API Manager is that you get a library of production-tested policies you can attach declaratively, instead of hand-rolling rate limiting or authentication logic inside every Mule application. Before applying any of them, it helps to know the full catalog and what problem each category solves.</p>\n\n<h2>The Policy Catalog</h2>\n<p>Policies in Anypoint Platform generally fall into a handful of families: traffic management, identity and access control, payload inspection, and message manipulation. The table below summarizes the most commonly used out-of-the-box policies.</p>\n\n<table>\n<thead><tr><th>Policy Category</th><th>What It Does</th><th>Typical Use Case</th></tr></thead>\n<tbody>\n<tr><td>Rate Limiting</td><td>Caps the number of requests a client can make within a fixed time window (e.g., 1000 requests/hour).</td><td>Protecting backend capacity from a single noisy consumer.</td></tr>\n<tr><td>Throttling</td><td>Similar to rate limiting but queues or smooths bursts rather than hard-rejecting every excess call.</td><td>Preventing sudden traffic spikes from overwhelming a downstream system.</td></tr>\n<tr><td>Spike Control</td><td>Limits the rate of requests processed per second regardless of client identity.</td><td>Guarding against traffic surges that could destabilize the Mule runtime itself.</td></tr>\n<tr><td>Client ID Enforcement</td><td>Requires a valid client_id/client_secret pair (or just client_id) issued via API Manager contracts.</td><td>Tracking and controlling which registered applications can call the API.</td></tr>\n<tr><td>OAuth 2.0 Token Enforcement</td><td>Validates bearer tokens against an external OAuth provider before allowing the request through.</td><td>Securing APIs consumed by applications that already integrate with an identity provider.</td></tr>\n<tr><td>JWT Validation</td><td>Validates the signature, expiration, and claims of a JSON Web Token presented in the request.</td><td>Stateless authentication where the token itself carries verifiable identity claims.</td></tr>\n<tr><td>IP Allowlist / Denylist</td><td>Restricts access based on CIDR-defined source IP ranges.</td><td>Locking an internal API down to a corporate network or blocking known bad actors.</td></tr>\n<tr><td>CORS</td><td>Controls which browser-based origins, methods, and headers are permitted to call the API.</td><td>APIs consumed directly by single-page applications running in a browser.</td></tr>\n<tr><td>Basic Authentication</td><td>Validates a username/password pair against a configured security manager.</td><td>Simple, low-friction protection for internal or legacy-style integrations.</td></tr>\n<tr><td>JSON / XML Threat Protection</td><td>Rejects maliciously structured payloads (deep nesting, huge arrays, entity expansion attacks).</td><td>Guarding parsers against denial-of-service and injection-style payloads.</td></tr>\n<tr><td>Header Injection / Removal</td><td>Adds, rewrites, or strips specific HTTP headers on the way in or out.</td><td>Enforcing security headers or stripping internal debug headers before a response leaves the gateway.</td></tr>\n<tr><td>Custom Policies</td><td>Organization-authored policies built with the Policy SDK for requirements the catalog doesn't cover.</td><td>Company-specific rules, such as validating a proprietary signature header.</td></tr>\n</tbody>\n</table>\n\n<h2>Grouping Policies by Purpose</h2>\n<p>It is useful to think of these less as a flat list and more as three functional groups. <strong>Traffic management</strong> policies (rate limiting, throttling, spike control) exist to protect the backend from load, not to authenticate anyone. <strong>Identity and access</strong> policies (client ID enforcement, OAuth 2.0, JWT validation, basic auth, IP allowlisting) decide who is allowed to call the API at all. <strong>Payload and message</strong> policies (threat protection, CORS, header manipulation) operate on the shape and content of the traffic itself, independent of who sent it.</p>\n\n<p>A typical production API instance layers several of these together &mdash; for example, client ID enforcement to identify the caller, rate limiting to protect capacity, and JSON threat protection to reject malformed bodies, all applied to the same instance simultaneously.</p>\n\n<h2>Example: A Minimal Rate Limiting Policy Configuration</h2>\n<pre><code class=\"language-yaml\">policyName: rate-limiting\nconfig:\n  rateLimits:\n    - id: default-tier\n      maximumRequests: 1000\n      timePeriodInMilliseconds: 3600000\n  clusterizable: true</code></pre>\n\n<h2>Choosing the Right Policy</h2>\n<p>When deciding which policy to apply, start by asking what you are actually protecting against. If the concern is backend load, reach for rate limiting or spike control. If the concern is unauthorized access, pick an identity policy that matches how your consumers already authenticate elsewhere in your organization &mdash; there is little value in introducing a new OAuth provider if every other API already validates JWTs issued by the same identity platform. If the concern is malformed or hostile payloads, threat protection policies belong on any publicly exposed endpoint that accepts JSON or XML bodies.</p>\n\n<ul>\n<li>Anypoint Platform policies are grouped into traffic management, identity/access, and payload/message families.</li>\n<li>Most production APIs combine several policy types on a single instance rather than relying on just one.</li>\n<li>Rate limiting and throttling protect backend capacity; they do not authenticate callers.</li>\n<li>Identity policies (client ID, OAuth 2.0, JWT, basic auth, IP allowlisting) determine who may call the API.</li>\n<li>Threat protection and header policies operate on the message content itself, regardless of caller identity.</li>\n<li>Custom policies fill gaps when no built-in policy matches an organization-specific requirement.</li>\n</ul>\n"
    },
    {
      "slug": "how-to-apply-a-policy",
      "title": "How to Apply a Policy: Step-by-Step",
      "meta_description": "A clear, step-by-step walkthrough of applying, configuring, and verifying a policy on an API instance in Anypoint API Manager, including policy ordering.",
      "reading_time": "6 min read",
      "summary": "Applying a policy in API Manager is a guided, no-code process, but verifying it actually took effect and understanding policy ordering both matter just as much.",
      "content_html": "\n<p>Applying a policy sounds like it should be a single click, and largely it is &mdash; but there are a few steps along the way that are easy to skip, and skipping them is usually what causes a policy to appear \"applied\" while doing nothing at runtime. Here is the full walkthrough.</p>\n\n<h2>Step-by-Step Walkthrough</h2>\n<ol>\n<li><strong>Log into Anypoint Platform.</strong> Sign in at anypoint.mulesoft.com with an account that has the API Manager permissions needed for the target business group and environment.</li>\n<li><strong>Navigate to API Manager.</strong> From the platform launcher, open API Manager. Confirm you are in the correct environment (Sandbox vs Production) using the environment selector, since API instances are environment-scoped.</li>\n<li><strong>Select the API instance.</strong> Click into the specific API instance you want to govern &mdash; this is the version of the API registered against a particular environment, not the API specification itself.</li>\n<li><strong>Open the Policies tab.</strong> Inside the instance, click the Policies tab to see any policies already applied and their current order.</li>\n<li><strong>Click Add Policy (or Apply New Policy).</strong> This opens the policy catalog, searchable by name or category.</li>\n<li><strong>Choose the policy.</strong> Select the policy you need, for example Rate Limiting &ndash; SLA Based or IP Allowlist.</li>\n<li><strong>Configure its parameters.</strong> Every policy exposes a configuration form specific to that policy &mdash; time windows and request counts for rate limiting, CIDR blocks for IP allowlisting, allowed origins for CORS, and so on. Fill these in deliberately; default values are rarely correct for production traffic.</li>\n<li><strong>Save and apply.</strong> Click Apply (or Save). Anypoint Platform propagates the policy to the runtime almost immediately for cloud-hosted gateways; self-hosted runtimes pick up changes on their next poll interval.</li>\n<li><strong>Verify the policy is active.</strong> Call the API instance directly and inspect the response. Many policies leave observable evidence: a rate limiting policy returns HTTP 429 once the limit is exceeded and includes <code>X-RateLimit-Limit</code> / <code>X-RateLimit-Remaining</code> headers; a CORS policy adds <code>Access-Control-Allow-Origin</code> to responses; an IP denylist returns HTTP 403 for a blocked address. If none of these show up, double-check that autodiscovery is correctly wired in the running application and that you tested against the correct environment's URL.</li>\n</ol>\n\n<h2>Policy Ordering and Precedence</h2>\n<p>When more than one policy is applied to the same API instance, order matters. Policies execute as a chain: each inbound request passes through every policy in sequence before reaching the implementation, and the response passes back through them in reverse. API Manager lets you drag and reorder policies in the Policies tab, and the order you choose has real consequences.</p>\n<p>A common mistake is placing a rate limiting policy after an expensive JSON threat protection policy that runs on every request, including ones that will ultimately be rejected for exceeding the rate limit. Placing lightweight, rejection-heavy policies (IP allowlist, client ID enforcement, rate limiting) earlier in the chain avoids doing unnecessary work on requests that are going to be blocked anyway. Security-critical policies such as authentication should generally run before anything that inspects or transforms the payload, so that unauthenticated requests never reach payload-processing logic.</p>\n\n<h2>Example: What a Verification Call Looks Like</h2>\n<pre><code class=\"language-bash\">curl -i https://your-org.api.anypoint.mulesoft.com/orders-api/v1/orders \\\n  -H \"client_id: abcd1234\" \\\n  -H \"client_secret: secretvalue\"\n\n# Expect, for example:\n# HTTP/1.1 429 Too Many Requests\n# X-RateLimit-Limit: 1000\n# X-RateLimit-Remaining: 0</code></pre>\n\n<h2>Applying Policies at Scale</h2>\n<p>For organizations managing dozens of API instances, applying policies one at a time through the UI does not scale well. Anypoint Platform supports applying policies via the Platform APIs and via Anypoint CLI, which is the more practical approach once policy application becomes part of a repeatable governance process rather than a one-off task.</p>\n\n<ul>\n<li>Policies are applied per API instance, which is environment-scoped &mdash; Sandbox and Production instances are configured independently.</li>\n<li>Every policy has a configuration form; leaving defaults unexamined is a common source of misconfigured policies.</li>\n<li>Applying a policy is not the same as confirming it works &mdash; always make a verification call afterward.</li>\n<li>Policy order matters: lightweight, rejection-prone policies should generally run before expensive or payload-inspecting ones.</li>\n<li>Authentication and access-control policies should typically execute before payload transformation or threat protection policies.</li>\n</ul>\n"
    },
    {
      "slug": "applying-policies",
      "title": "Applying Policies",
      "meta_description": "Learn how to apply, configure, and order out-of-the-box and custom API policies in Anypoint API Manager to secure and control API traffic.",
      "reading_time": "6 min read",
      "summary": "Policies in API Manager let you enforce security, traffic management, and compliance rules on an API instance without modifying its implementation.",
      "content_html": "\n<p>A <strong>policy</strong> in Anypoint API Manager is a reusable rule that gets applied to an API instance and enforced automatically at runtime by the API gateway (either the Mule runtime itself, via autodiscovery, or a dedicated gateway proxy). Policies are the mechanism that turns a plain Mule application into a governed, secured API.</p>\n\n<h2>Categories of Policies</h2>\n<ul>\n<li><strong>Security</strong> &mdash; Client ID Enforcement, OAuth 2.0 Access Token Enforcement, Basic Authentication, IP Whitelisting/Blacklisting, JWT validation.</li>\n<li><strong>Compliance</strong> &mdash; Header injection/removal, CORS.</li>\n<li><strong>Traffic management</strong> &mdash; Rate Limiting, Throttling, SLA-based Rate Limiting, Spike Control.</li>\n<li><strong>Transformation</strong> &mdash; Message logging, header manipulation.</li>\n</ul>\n\n<h2>Applying a Policy</h2>\n<p>In API Manager, open the target API instance, go to the <strong>Policies</strong> tab, and choose <strong>Apply New Policy</strong>. Select a policy from the catalog (e.g., Client ID Enforcement), configure its parameters, and save. There is no code change and no redeploy of the Mule application &mdash; the gateway picks up the new policy configuration automatically, typically within a few seconds to a couple of minutes.</p>\n\n<h2>Example: Rate Limiting Policy Configuration</h2>\n<p>Most out-of-the-box policies are configured through the API Manager UI, but conceptually the resulting policy configuration looks like this:</p>\n<pre><code class=\"language-xml\">&lt;rate-limiting-policy&gt;\n  &lt;rate-limits&gt;\n    &lt;rate-limit\n      timePeriodInMilliseconds=\"60000\"\n      maximumRequests=\"100\" /&gt;\n  &lt;/rate-limits&gt;\n  &lt;cluster-aware&gt;true&lt;/cluster-aware&gt;\n&lt;/rate-limiting-policy&gt;</code></pre>\n\n<h2>Policy Order Matters</h2>\n<p>When multiple policies are applied to the same API instance, they execute in a defined order, and you can reorder them in API Manager. As a general rule:</p>\n<ol>\n<li>Security/authentication policies should run first, so unauthenticated traffic is rejected before consuming further processing.</li>\n<li>Traffic management policies (rate limiting, throttling, spike control) typically run next.</li>\n<li>Transformation/logging policies usually run last, closest to the backend.</li>\n</ol>\n<p>Getting the order wrong can create real problems &mdash; for example, applying a rate-limiting policy before a client-ID-enforcement policy would count anonymous, unauthenticated requests against your rate limit budget.</p>\n\n<h2>Scope: Applying Policies to a Subset of Resources</h2>\n<p>Many policies support scoping to specific API resources and methods rather than the whole API, letting you, for example, enforce OAuth only on write operations (POST/PUT/DELETE) while leaving read-only GET endpoints publicly accessible.</p>\n\n<h2>Custom Policies</h2>\n<p>When the out-of-the-box catalog does not cover a requirement, MuleSoft supports building <strong>custom policies</strong> using the Policy SDK, packaged and published to an organization's private policy catalog inside Exchange, then applied exactly like a standard policy from API Manager.</p>\n\n<h2>Environment-Level vs API-Level Policies</h2>\n<p>Some policies (e.g., basic compliance or security baselines) can be applied at the <strong>environment level</strong> so that every API instance in that environment automatically inherits them, in addition to any API-specific policies layered on top. This is a common way for platform teams to enforce organization-wide standards.</p>\n\n<blockquote>Note: A policy applied but misconfigured (e.g., an overly permissive rate limit) will not throw a build error &mdash; always verify behavior by testing the live endpoint after applying or changing a policy.</blockquote>\n\n<h2>Key Points</h2>\n<ul>\n<li>Policies are applied to API instances in API Manager and enforced at runtime without redeploying code.</li>\n<li>Policy execution order matters &mdash; security policies generally should run before traffic-management policies.</li>\n<li>Policies can be scoped to specific resources/methods, not just the whole API.</li>\n<li>Custom policies extend the out-of-the-box catalog via the Policy SDK.</li>\n<li>Environment-level policies let platform teams enforce standards across every API automatically.</li>\n</ul>\n"
    },
    {
      "slug": "client-id-enforcement",
      "title": "Client ID Enforcement",
      "meta_description": "Learn how the Client ID Enforcement policy secures APIs by requiring a valid client_id and client_secret pair, and how to configure it in API Manager.",
      "reading_time": "5 min read",
      "summary": "Client ID Enforcement is a security policy that requires every request to present a valid client ID and secret issued by Anypoint Platform.",
      "content_html": "\n<p>Client ID Enforcement is one of the simplest and most commonly applied security policies in API Manager. It requires every incoming request to present a valid <code>client_id</code> (and optionally a <code>client_secret</code>) that was issued when a client application requested access to the API through a contract. Requests without valid credentials are rejected before they ever reach your backend logic.</p>\n\n<h2>How It Works</h2>\n<p>When a consuming application requests access to an API in Exchange/API Manager, and that request is approved (either manually or automatically depending on the SLA tier), Anypoint Platform issues a unique <code>client_id</code>/<code>client_secret</code> pair for that specific client application and API contract. The Client ID Enforcement policy checks incoming requests for these credentials, typically as query parameters or headers, and validates them against the platform's records before allowing the request through.</p>\n\n<h2>Passing Credentials</h2>\n<p>Depending on configuration, credentials can be sent as:</p>\n<ul>\n<li>Query parameters: <code>?client_id=abc123&amp;client_secret=xyz789</code></li>\n<li>HTTP headers: <code>client_id</code> and <code>client_secret</code></li>\n</ul>\n<pre><code class=\"language-bash\">curl -X GET \"https://orders-api.us-e1.cloudhub.io/orders\"   -H \"client_id: 8f9a2c1e4b6d\"   -H \"client_secret: 4d7e1a9c8b3f\"</code></pre>\n\n<h2>Applying the Policy</h2>\n<p>In API Manager, open the API instance, go to Policies, choose <strong>Client ID Enforcement</strong>, and select whether credentials are expected as headers or query parameters. No backend code changes are required &mdash; the gateway intercepts and validates the request before it reaches your flow.</p>\n\n<h2>Why Use It</h2>\n<ul>\n<li><strong>Identify the caller</strong> &mdash; even without full OAuth, you know exactly which registered client application made each request, which is essential for analytics, throttling per consumer, and auditing.</li>\n<li><strong>Block anonymous traffic</strong> &mdash; only applications that went through the contract/approval process can call the API at all.</li>\n<li><strong>Pair with SLA tiers</strong> &mdash; because each request is tied to a known client, rate limits can be applied per client rather than globally.</li>\n</ul>\n\n<h2>Client ID Enforcement vs OAuth 2.0</h2>\n<table>\n<thead><tr><th>Aspect</th><th>Client ID Enforcement</th><th>OAuth 2.0</th></tr></thead>\n<tbody>\n<tr><td>What it proves</td><td>Which application is calling</td><td>Which application AND which authorized user/scope</td></tr>\n<tr><td>Complexity</td><td>Low &mdash; no token exchange flow</td><td>Higher &mdash; requires an authorization server and token lifecycle</td></tr>\n<tr><td>Typical use case</td><td>System-to-system / internal APIs, simple partner access</td><td>User-delegated access, externally facing APIs needing fine-grained scopes</td></tr>\n<tr><td>Credential lifetime</td><td>Static client_id/secret until revoked</td><td>Short-lived access tokens, refreshable</td></tr>\n</tbody>\n</table>\n\n<h2>Best Practices</h2>\n<ul>\n<li>Prefer headers over query parameters for credentials in production &mdash; query parameters can end up logged in proxies, browser history, or access logs.</li>\n<li>Combine Client ID Enforcement with TLS (HTTPS) so credentials are never sent in clear text.</li>\n<li>Rotate client secrets periodically and immediately revoke credentials for any decommissioned client application.</li>\n<li>Use Client ID Enforcement together with rate limiting or SLA tiers rather than as a security control on its own for externally facing APIs &mdash; on its own it does not encrypt or fully authenticate a request the way OAuth does.</li>\n</ul>\n\n<blockquote>Tip: Client ID Enforcement is often the fastest way to get baseline API security in place during early rollout, with a migration to OAuth 2.0 planned as the API matures toward broader external consumption.</blockquote>\n\n<h2>Key Points</h2>\n<ul>\n<li>Client ID Enforcement validates a client_id/client_secret pair issued through an approved API contract.</li>\n<li>Credentials can be passed as headers (recommended) or query parameters.</li>\n<li>It identifies the calling application but does not provide user-level authorization the way OAuth 2.0 does.</li>\n<li>It pairs naturally with SLA tiers and rate limiting for per-client traffic control.</li>\n</ul>\n"
    },
    {
      "slug": "oauth2-in-mulesoft",
      "title": "OAuth 2.0 in MuleSoft",
      "meta_description": "Understand how OAuth 2.0 works in Anypoint Platform: grant types, token enforcement policies, and configuring an external or Anypoint-provided authorization server.",
      "reading_time": "7 min read",
      "summary": "OAuth 2.0 in MuleSoft is enforced through API Manager policies that validate access tokens issued by an authorization server before allowing a request through.",
      "content_html": "\n<p>OAuth 2.0 is the industry-standard authorization framework, and MuleSoft supports it both as a client (when a Mule application needs to call an OAuth-protected external API) and as a resource server (when MuleSoft-managed APIs themselves need to be protected by OAuth). Understanding both directions is essential for real-world integration work.</p>\n\n<h2>OAuth 2.0 Roles, Quickly</h2>\n<ul>\n<li><strong>Resource Owner</strong> &mdash; the user who owns the protected data.</li>\n<li><strong>Client</strong> &mdash; the application requesting access (could be a Mule app or an external consumer).</li>\n<li><strong>Authorization Server</strong> &mdash; issues access tokens after authenticating the resource owner/client (e.g., Okta, Azure AD, PingFederate, or Anypoint's own OAuth provider).</li>\n<li><strong>Resource Server</strong> &mdash; the API that accepts the access token and serves the protected resource.</li>\n</ul>\n\n<h2>Protecting a MuleSoft API with OAuth 2.0</h2>\n<p>To require OAuth 2.0 on an API managed through API Manager, you apply the <strong>OAuth 2.0 Access Token Enforcement</strong> policy to the API instance. You configure it to point at your OAuth provider (either Anypoint Platform's built-in provider or an external one like PingFederate, Okta, or Azure AD) so the gateway can validate incoming access tokens on every request.</p>\n<pre><code class=\"language-bash\">curl -X GET \"https://orders-api.us-e1.cloudhub.io/orders\"   -H \"Authorization: Bearer eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...\"</code></pre>\n<p>The gateway extracts the bearer token, validates it against the configured authorization server (checking signature, expiration, and often scope), and only forwards the request to the backend flow if the token is valid.</p>\n\n<h2>Calling an OAuth-Protected External API from Mule</h2>\n<p>When your Mule application is the client consuming an external OAuth-protected API, the HTTP Requester connector supports OAuth 2.0 out of the box for common grant types, automatically handling the token request and refresh.</p>\n<pre><code class=\"language-xml\">&lt;http:request-config name=\"External_API_Config\"&gt;\n  &lt;http:request-connection host=\"api.partner.com\" port=\"443\" protocol=\"HTTPS\"&gt;\n    &lt;http:authentication&gt;\n      &lt;http:oauth-client-credentials-grant-type\n        clientId=\"${oauth.clientId}\"\n        clientSecret=\"${oauth.clientSecret}\"\n        tokenUrl=\"https://auth.partner.com/oauth/token\" /&gt;\n    &lt;/http:authentication&gt;\n  &lt;/http:request-connection&gt;\n&lt;/http:request-config&gt;</code></pre>\n\n<h2>Common Grant Types Used in Integration</h2>\n<table>\n<thead><tr><th>Grant Type</th><th>Typical Use Case</th></tr></thead>\n<tbody>\n<tr><td>Client Credentials</td><td>System-to-system calls with no end user involved (most common for backend integrations)</td></tr>\n<tr><td>Authorization Code</td><td>A user logs in and delegates access to an application (web/mobile apps)</td></tr>\n<tr><td>Refresh Token</td><td>Used alongside Authorization Code to obtain new access tokens without re-authenticating the user</td></tr>\n</tbody>\n</table>\n<p>The Client Credentials grant is by far the most common pattern for Mule applications integrating server-to-server, since there is no interactive user to redirect through a login screen.</p>\n\n<h2>Token Validation Approaches</h2>\n<ul>\n<li><strong>Token introspection</strong> &mdash; the gateway calls the authorization server's introspection endpoint on every request (or a cached interval) to check token validity.</li>\n<li><strong>JWT validation</strong> &mdash; if the authorization server issues self-contained JWT access tokens, the gateway can validate the signature and claims locally without a network call for every request, reducing latency.</li>\n</ul>\n\n<h2>Using Anypoint Platform's Built-in OAuth Provider</h2>\n<p>For simpler use cases, Anypoint Platform can act as the OAuth authorization server itself, issuing and managing tokens tied to registered client applications, without standing up a separate identity provider. Larger enterprises typically prefer integrating with an existing corporate identity provider instead, for centralized user and credential management.</p>\n\n<blockquote>Tip: Combine OAuth 2.0 Access Token Enforcement with a rate-limiting or SLA-tier policy so that even authenticated clients cannot overwhelm the backend.</blockquote>\n\n<h2>Key Points</h2>\n<ul>\n<li>OAuth 2.0 in MuleSoft applies both when Mule calls external APIs (as a client) and when MuleSoft-managed APIs are protected (as a resource server).</li>\n<li>API Manager's OAuth 2.0 Access Token Enforcement policy validates bearer tokens against a configured authorization server.</li>\n<li>The HTTP Requester connector natively supports common OAuth grant types, including automatic token refresh.</li>\n<li>Client Credentials is the most common grant type for server-to-server integration flows.</li>\n</ul>\n"
    },
    {
      "slug": "ip-allowlist-denylist-policy",
      "title": "IP Allowlist and Denylist Policies",
      "meta_description": "How CIDR-based IP allowlist and denylist policies work in Anypoint API Manager, their key configuration parameters, and the most common real-world use cases.",
      "reading_time": "5 min read",
      "summary": "IP allowlist and denylist policies restrict API access by source address using CIDR ranges, offering a coarse but effective first line of defense.",
      "content_html": "\n<p>Not every access-control decision needs to involve tokens or credentials. Sometimes the simplest and most robust control is: only let requests through if they are coming from a known network. That is precisely what the IP Allowlist and IP Denylist policies do in API Manager.</p>\n\n<h2>How CIDR-Based Filtering Works</h2>\n<p>Both policies work by comparing the source IP address of an inbound request against a list of CIDR (Classless Inter-Domain Routing) ranges you configure. A CIDR range like <code>10.20.0.0/16</code> represents every address from <code>10.20.0.0</code> to <code>10.20.255.255</code>, letting you describe an entire corporate subnet with a single entry rather than listing thousands of individual addresses.</p>\n<p>The <strong>allowlist</strong> (whitelist) policy takes the opposite approach from the <strong>denylist</strong> (blacklist) policy: an allowlist rejects everything except the ranges you explicitly permit, while a denylist permits everything except the ranges you explicitly block. You typically use one or the other on a given instance, not both, since they express opposite default postures.</p>\n\n<h2>Configuration Parameters</h2>\n<p>Both policies share a similar shape: a list of CIDR blocks, and a setting for how the client's real IP address should be determined. That second setting matters more than it looks &mdash; if the API sits behind a load balancer or CDN, the raw TCP connection IP will be the load balancer's address, not the original caller's. In that case the policy needs to be configured to read the address from a forwarding header such as <code>X-Forwarded-For</code> instead.</p>\n\n<pre><code class=\"language-yaml\">policyName: ip-allowlist\nconfig:\n  ipExpression: \"#[attributes.headers['x-forwarded-for']]\"\n  cidrBlocks:\n    - \"10.20.0.0/16\"\n    - \"192.168.100.0/24\"</code></pre>\n\n<pre><code class=\"language-yaml\">policyName: ip-denylist\nconfig:\n  ipExpression: \"#[attributes.remoteAddress]\"\n  cidrBlocks:\n    - \"203.0.113.45/32\"\n    - \"198.51.100.0/24\"</code></pre>\n\n<h2>Common Use Cases</h2>\n<p>The allowlist policy is the natural fit for internal APIs that should never be reachable from outside the corporate network or a trusted set of partner networks &mdash; for example, an internal HR system's API that should only ever be called from the company's VPN egress range. Restricting access this way means that even if a client ID were somehow leaked, the request still could not reach the API from an unauthorized network.</p>\n<p>The denylist policy fits a different scenario: a public-facing API that should remain open to the general internet, but where a specific range has been identified as a source of abuse &mdash; a known botnet range, a compromised partner network, or an IP that has been repeatedly triggering rate limits and appears to be scraping. Rather than tightening rate limits for everyone, you block the offending range directly.</p>\n\n<h2>Limitations to Keep in Mind</h2>\n<p>IP-based filtering is coarse. It says nothing about who a request actually comes from within an allowed range, and it can be circumvented by anyone routing traffic through an already-trusted network (for example, a compromised machine on the corporate VPN). For that reason, IP allowlisting is best treated as a network-layer complement to identity-based policies like client ID enforcement or OAuth 2.0, not a replacement for them. It is also worth remembering that CIDR ranges for cloud-hosted clients (offices behind dynamic ISP addresses, for instance) can change without notice, so allowlists tied to non-static ranges require ongoing maintenance.</p>\n\n<ul>\n<li>IP allowlist and denylist policies filter requests using CIDR-notation address ranges.</li>\n<li>Allowlist denies by default and only permits configured ranges; denylist permits by default and blocks configured ranges.</li>\n<li>Behind a load balancer or CDN, configure the policy to read the client IP from a forwarding header, not the raw connection address.</li>\n<li>Allowlisting suits internal or partner-only APIs; denylisting suits public APIs where specific bad actors need to be blocked.</li>\n<li>IP filtering is a network-layer control and works best paired with identity-based policies, not as a standalone security measure.</li>\n</ul>\n"
    },
    {
      "slug": "cors-policy",
      "title": "CORS Policy",
      "meta_description": "What CORS is, why browser-based API clients need it, and how to configure allowed origins, methods, headers, and credentials in the CORS policy correctly.",
      "reading_time": "6 min read",
      "summary": "The CORS policy tells browsers which origins, methods, and headers are permitted to call an API directly from client-side JavaScript.",
      "content_html": "\n<p>If an API is ever going to be called directly from a browser-based application &mdash; a React single-page app, an Angular admin console, anything running client-side JavaScript on a different domain than the API itself &mdash; CORS is not optional. Without it, the browser will silently block the response before your application code ever sees it.</p>\n\n<h2>What CORS Actually Is</h2>\n<p>Cross-Origin Resource Sharing (CORS) is a browser security mechanism, not a server-side concept. Browsers enforce the same-origin policy by default: JavaScript running on <code>https://app.example.com</code> cannot read a response from <code>https://api.example.com</code> unless the API explicitly says it is allowed to. The API says this by returning specific response headers, and CORS support in API Manager is essentially a policy that generates those headers automatically, including handling the preflight <code>OPTIONS</code> request that browsers send before certain kinds of cross-origin calls.</p>\n<p>It is important to understand that CORS is not a security boundary against server-to-server calls, curl, or Postman &mdash; those never enforce it. CORS only matters for requests initiated by browser JavaScript.</p>\n\n<h2>Key Configuration Parameters</h2>\n<table>\n<thead><tr><th>Parameter</th><th>Purpose</th></tr></thead>\n<tbody>\n<tr><td>Allowed Origins</td><td>Which origins (scheme + domain + port) may make cross-origin requests. Can be a specific list or a wildcard.</td></tr>\n<tr><td>Allowed Methods</td><td>Which HTTP verbs (GET, POST, PUT, DELETE, etc.) are permitted from allowed origins.</td></tr>\n<tr><td>Allowed Headers</td><td>Which request headers a browser client is permitted to send, e.g. Content-Type, Authorization.</td></tr>\n<tr><td>Exposed Headers</td><td>Which response headers the browser's JavaScript is allowed to read beyond the small default set.</td></tr>\n<tr><td>Max Age</td><td>How long (in seconds) the browser may cache a preflight response before sending another OPTIONS check.</td></tr>\n<tr><td>Allow Credentials</td><td>Whether cookies or Authorization headers can be included in cross-origin requests; cannot be combined with a wildcard origin.</td></tr>\n</tbody>\n</table>\n\n<h2>A Realistic Configuration</h2>\n<p>Consider an internal dashboard hosted at <code>https://dashboard.internal.example.com</code> that calls an Orders API and needs to send an Authorization header and read a custom pagination header back.</p>\n\n<pre><code class=\"language-yaml\">policyName: cors\nconfig:\n  origins:\n    - \"https://dashboard.internal.example.com\"\n  accessControlMaxAge: 3600\n  exposeHeaders:\n    - \"X-Total-Count\"\n  allowCredentials: true\n  publicResource: false\n  methods:\n    - GET\n    - POST\n    - PUT\n  headers:\n    - \"Content-Type\"\n    - \"Authorization\"</code></pre>\n\n<h2>Common Pitfalls</h2>\n<p>The most frequent mistake is using a wildcard (<code>*</code>) for Allowed Origins on an API that also needs <code>Allow Credentials</code> enabled &mdash; browsers explicitly reject that combination, since it would let any site on the internet make authenticated requests on a user's behalf. If credentials are required, origins must be listed explicitly.</p>\n<p>Another common issue is forgetting that any custom response header the client-side code needs to read &mdash; pagination counts, rate limit headers, request IDs &mdash; must be listed under Exposed Headers. Browsers only expose a small default set (like <code>Content-Type</code>) to JavaScript unless told otherwise; the header can be present in the raw HTTP response and still be invisible to <code>fetch()</code> or <code>XMLHttpRequest</code> code if it isn't exposed.</p>\n<p>Finally, remember that the CORS policy governs preflight and header behavior, but it does not replace authentication. A CORS-enabled API without a client ID or token enforcement policy is still fully open to any caller that isn't a browser.</p>\n\n<ul>\n<li>CORS is enforced by browsers, not servers; it has no effect on server-to-server or command-line calls.</li>\n<li>Allowed Origins, Methods, and Headers control what a browser will permit a page to send; Exposed Headers controls what JavaScript can read back.</li>\n<li>Wildcard origins cannot be combined with Allow Credentials &mdash; browsers will reject that combination outright.</li>\n<li>Any custom response header the front end needs must be explicitly listed in Exposed Headers.</li>\n<li>CORS configuration does not authenticate callers; pair it with an identity policy for actual access control.</li>\n</ul>\n"
    },
    {
      "slug": "threat-protection-policies",
      "title": "JSON and XML Threat Protection Policies",
      "meta_description": "How JSON and XML threat protection policies guard parsers against denial-of-service and injection-style attacks, with key configuration limits explained.",
      "reading_time": "6 min read",
      "summary": "JSON and XML threat protection policies inspect payload structure before parsing to block deeply nested, oversized, or maliciously crafted documents.",
      "content_html": "\n<p>Parsers are one of the least examined attack surfaces in an API. A well-formed but maliciously structured JSON or XML document can consume enormous amounts of memory or CPU before your application logic ever runs, simply by exploiting how parsers work. The JSON and XML Threat Protection policies exist to reject those payloads at the gateway, before they reach the runtime's parser.</p>\n\n<h2>JSON Threat Protection</h2>\n<p>JSON threat protection inspects the structural shape of an inbound JSON body against configured limits, rejecting anything that exceeds them with an HTTP 400 before the payload is ever deserialized into a Mule message.</p>\n<table>\n<thead><tr><th>Parameter</th><th>What It Guards Against</th></tr></thead>\n<tbody>\n<tr><td>Maximum object entry count</td><td>Objects with thousands of keys designed to exhaust memory during parsing.</td></tr>\n<tr><td>Maximum array element count</td><td>Enormous arrays that cause excessive allocation or slow downstream processing.</td></tr>\n<tr><td>Maximum string value length</td><td>Extremely long string values used to bloat memory or overflow buffers.</td></tr>\n<tr><td>Maximum nesting depth</td><td>Deeply nested objects/arrays that cause stack exhaustion in recursive parsers.</td></tr>\n<tr><td>Maximum key length</td><td>Abnormally long property names used for the same exhaustion effect as long values.</td></tr>\n</tbody>\n</table>\n\n<pre><code class=\"language-yaml\">policyName: json-threat-protection\nconfig:\n  maxObjectEntryCount: 200\n  maxArrayElementCount: 1000\n  maxStringValueLength: 10000\n  maxKeyLength: 128\n  maxDocumentDepth: 20</code></pre>\n\n<h2>XML Threat Protection</h2>\n<p>XML brings its own class of attacks, the most notorious being the \"billion laughs\" entity expansion attack, where a tiny document defines nested entities that reference each other repeatedly, expanding into gigabytes of data once parsed. XML threat protection blocks this and related XML External Entity (XXE) attacks by limiting entity expansion and attribute counts, and by disallowing external entity resolution outright.</p>\n<table>\n<thead><tr><th>Parameter</th><th>What It Guards Against</th></tr></thead>\n<tbody>\n<tr><td>Maximum entity expansions</td><td>Billion-laughs style attacks using nested entity definitions.</td></tr>\n<tr><td>External entity / DTD resolution</td><td>XXE attacks that read local files or make server-side requests via crafted DOCTYPE declarations.</td></tr>\n<tr><td>Maximum attribute count per element</td><td>Elements with thousands of attributes intended to slow or crash the parser.</td></tr>\n<tr><td>Maximum document depth</td><td>Deeply nested elements causing stack exhaustion, similar to the JSON case.</td></tr>\n</tbody>\n</table>\n\n<pre><code class=\"language-yaml\">policyName: xml-threat-protection\nconfig:\n  expandInternalEntities: false\n  expandExternalEntities: false\n  resolveExternalEntities: false\n  maxAttributeCountPerElement: 20\n  maxDocumentDepth: 20</code></pre>\n\n<h2>Why Structural Checks Happen Before Parsing</h2>\n<p>The key design point of both policies is that they inspect the raw payload's structure &mdash; not its business content &mdash; before a full parse is attempted, using lightweight streaming checks. This ordering matters: a naive defense that tries to parse the document first and validate afterward has already paid the CPU/memory cost the attack was designed to inflict. Threat protection policies are deliberately placed early in the policy chain for exactly this reason, alongside authentication policies, so hostile payloads are rejected as cheaply as possible.</p>\n\n<h2>Setting Sensible Limits</h2>\n<p>Limits should be set based on what legitimate traffic actually looks like, not arbitrary defaults. An API that only ever receives flat, shallow order records does not need a nesting depth of 50; setting it closer to 5&ndash;10 both blocks abuse and catches integration bugs (like accidental circular serialization) earlier. Conversely, setting limits too aggressively will start rejecting valid large-but-legitimate payloads, such as bulk upload endpoints, so these policies are often applied per-instance rather than globally.</p>\n\n<ul>\n<li>Threat protection policies inspect structure before full parsing, rejecting hostile payloads cheaply.</li>\n<li>JSON threat protection limits object size, array length, string length, key length, and nesting depth.</li>\n<li>XML threat protection blocks entity expansion (billion laughs) and external entity resolution (XXE), plus attribute and depth limits.</li>\n<li>These policies should run early in the policy chain, alongside authentication.</li>\n<li>Limits should reflect realistic payload shapes for the specific API, not one-size-fits-all defaults.</li>\n</ul>\n"
    },
    {
      "slug": "basic-authentication-policy",
      "title": "Basic Authentication (Simple Security Manager) Policy",
      "meta_description": "How to configure the Basic Authentication policy backed by a Simple Security Manager, and when it makes more practical sense than using OAuth 2.0 instead.",
      "reading_time": "5 min read",
      "summary": "The Basic Authentication policy validates a username and password against a configured Simple Security Manager, offering low-friction protection for simpler integrations.",
      "content_html": "\n<p>Not every API needs the full machinery of OAuth 2.0 token issuance and validation. For internal tools, legacy-style integrations, or quick proof-of-concept work, HTTP Basic Authentication backed by a Simple Security Manager remains a practical, low-friction option in API Manager's policy catalog.</p>\n\n<h2>How the Policy Works</h2>\n<p>The Basic Authentication policy expects the client to send credentials via the standard HTTP <code>Authorization: Basic &lt;base64(username:password)&gt;</code> header. When a request arrives, the gateway decodes the header and checks the username/password pair against a <strong>Simple Security Manager</strong> &mdash; a static, configured list of valid users rather than an external identity provider. If the credentials don't match, the gateway returns HTTP 401 with a <code>WWW-Authenticate: Basic</code> challenge header before the request ever reaches the backend implementation.</p>\n<p>Because the entire check happens at the gateway layer, the backend application never has to implement any authentication logic itself &mdash; it can simply trust that any request reaching it has already been validated.</p>\n\n<h2>Configuring the Policy</h2>\n<p>The Simple Security Manager configuration lists valid username/password pairs (or references a securely stored properties file rather than embedding plaintext passwords directly in the policy configuration). The policy itself is then applied against that security manager.</p>\n\n<pre><code class=\"language-yaml\"># Simple Security Manager users configuration\nsecurityManager:\n  users:\n    - username: reporting-service\n      password: \"${secure::reporting.service.password}\"\n    - username: batch-integration\n      password: \"${secure::batch.integration.password}\"</code></pre>\n\n<pre><code class=\"language-yaml\">policyName: basic-authentication\nconfig:\n  securityManagerName: internal-users-security-manager\n  realm: \"Orders API\"</code></pre>\n\n<h2>Where Credentials Are Validated</h2>\n<p>Validation happens entirely at the API gateway / policy layer, before the request reaches any Mule flow logic in the implementation. This is an important architectural point: the credentials are never seen by application code, which means rotating a password or adding a new consumer is purely an API Manager configuration change, with no redeployment of the underlying application required.</p>\n\n<h2>Basic Auth vs OAuth 2.0</h2>\n<p>The two solve overlapping but different problems, and the choice usually comes down to who the consumer is and what infrastructure already exists.</p>\n<ul>\n<li><strong>Use Basic Authentication</strong> when the set of consumers is small and known in advance (internal services, a handful of partner integrations), when there is no existing OAuth provider to integrate with, or when you need something running quickly for a non-production or low-risk scenario.</li>\n<li><strong>Use OAuth 2.0</strong> when consumers are external, numerous, or dynamic; when tokens need to expire and be refreshed without sharing long-lived static credentials; when you need scoped access rather than all-or-nothing access; or when the organization already has an identity provider that other APIs authenticate against, and consistency matters.</li>\n</ul>\n<p>Basic Authentication also has a real security tradeoff worth naming: the credentials are static and sent on every request (base64-encoded, not encrypted &mdash; TLS is what protects them in transit). There is no built-in expiration or rotation mechanism the way OAuth tokens have, so password rotation is a manual, coordinated process between the API owner and every consumer using that credential.</p>\n\n<h2>When It's the Right Call</h2>\n<p>Basic Authentication is a reasonable, deliberate choice &mdash; not just a shortcut &mdash; for machine-to-machine integrations behind a firewall, for internal batch jobs calling internal APIs, or as a stopgap while a longer-term OAuth integration is being built out. It becomes a liability mainly when it's left in place for externally facing, high-value APIs where credential leakage has broad consequences.</p>\n\n<ul>\n<li>Basic Authentication validates credentials from the standard HTTP Authorization header against a Simple Security Manager.</li>\n<li>All validation happens at the gateway/policy layer; the backend implementation never handles credentials directly.</li>\n<li>It suits internal, known, and limited sets of consumers better than large or external ones.</li>\n<li>OAuth 2.0 is generally preferable for external consumers, scoped access, and token expiration/rotation.</li>\n<li>Credentials should always be stored using secure property placeholders, never in plaintext configuration.</li>\n</ul>\n"
    },
    {
      "slug": "custom-policies",
      "title": "Custom Policies with the Policy SDK",
      "meta_description": "An overview of the Policy SDK for building custom Anypoint policies, its YAML definition plus implementation structure, and when a team should build one.",
      "reading_time": "6 min read",
      "summary": "The Policy SDK lets teams package organization-specific enforcement logic as a reusable policy when no built-in policy fits the requirement.",
      "content_html": "\n<p>The out-of-the-box policy catalog covers the vast majority of common needs, but occasionally a requirement is specific enough to an organization &mdash; a proprietary signature scheme, a legacy header format, a bespoke rate-limiting rule tied to internal account tiers &mdash; that no built-in policy fits. That's the gap the Policy SDK closes: it lets a team build, package, and publish its own policy that behaves exactly like a built-in one inside API Manager.</p>\n\n<h2>What a Custom Policy Consists Of</h2>\n<p>A custom policy has two parts working together: a <strong>policy definition</strong> written in YAML, which declares the policy's metadata, configurable parameters, and the extension points it hooks into; and an <strong>implementation</strong>, typically a Mule XML fragment (or a compiled extension for more advanced cases) that actually performs the enforcement logic. Once built, the pair is packaged and published to Exchange, after which it appears in the API Manager policy catalog exactly like any Anypoint-provided policy &mdash; searchable, configurable through a generated form, and appliable to any API instance.</p>\n\n<h2>High-Level Structure of the Definition YAML</h2>\n<pre><code class=\"language-yaml\"># policy-definition.yaml\nid: custom-signature-validation\nname: Custom Signature Validation\ndescription: Validates a proprietary HMAC signature header on inbound requests.\ncategory: Security\ntype: custom\nresourceLevelSupported: true\nconfiguration:\n  - propertyName: sharedSecretName\n    name: Shared Secret Property\n    type: string\n    optional: false\n  - propertyName: headerName\n    name: Signature Header Name\n    type: string\n    defaultValue: \"X-Signature\"\nencryption:\n  properties:\n    - sharedSecretName</code></pre>\n\n<h2>High-Level Structure of the Implementation</h2>\n<p>The implementation file references the parameters declared above and defines the actual Mule logic executed for every request &mdash; typically wrapped so it runs before the request reaches the backend flow, similar to how a built-in policy intercepts traffic.</p>\n<pre><code class=\"language-xml\">&lt;mule xmlns=\"http://www.mulesoft.org/schema/mule/core\"\n      xmlns:http-policy=\"http://www.mulesoft.org/schema/mule/http-policy\"&gt;\n\n  &lt;http-policy:proxy name=\"custom-signature-validation\"&gt;\n    &lt;http-policy:source&gt;\n      &lt;choice&gt;\n        &lt;when expression=\"#[not (attributes.headers['{{headerName}}']? default false)]\"&gt;\n          &lt;raise-error type=\"HTTP:UNAUTHORIZED\" description=\"Missing signature header\"/&gt;\n        &lt;/when&gt;\n        &lt;otherwise&gt;\n          &lt;http-policy:execute-next/&gt;\n        &lt;/otherwise&gt;\n      &lt;/choice&gt;\n    &lt;/http-policy:source&gt;\n  &lt;/http-policy:proxy&gt;\n\n&lt;/mule&gt;</code></pre>\n\n<h2>Building and Publishing</h2>\n<p>Custom policies are typically developed using the Policy SDK's Maven archetype, tested locally, and then packaged and deployed to Exchange within the organization's business group using the Anypoint CLI or the associated Maven plugin. Once published, the policy becomes available (scoped to that organization, not the whole platform) in API Manager just like any other policy, complete with an auto-generated configuration form driven by the YAML's <code>configuration</code> section.</p>\n\n<h2>When a Team Should Build One</h2>\n<p>Building a custom policy is worth the investment when a rule needs to be enforced consistently across many APIs and many teams &mdash; if it's a one-off need for a single API, it is often simpler to implement the logic directly inside that API's flows rather than build, test, version, and maintain a reusable policy artifact. Good candidates include organization-wide header/signature validation schemes, custom rate-limiting logic tied to internal business rules that don't map to the standard SLA-tier model, or translating a legacy authentication scheme that the built-in policies don't natively support.</p>\n<p>Before building one, it's worth checking whether combining two or three existing policies (say, header injection plus IP allowlisting) already achieves the same outcome &mdash; custom policies add a maintenance burden (versioning, testing, backward compatibility) that a combination of standard policies avoids entirely.</p>\n\n<ul>\n<li>Custom policies consist of a YAML policy definition and a separate implementation, packaged together and published to Exchange.</li>\n<li>Once published, a custom policy appears in the API Manager catalog and configures like any built-in policy.</li>\n<li>The definition YAML declares configurable parameters that drive an auto-generated configuration form.</li>\n<li>Custom policies are best built for requirements shared across many APIs, not one-off needs.</li>\n<li>Check whether existing policies can be combined to meet the requirement before investing in a custom one.</li>\n</ul>\n"
    },
    {
      "slug": "rate-limiting-and-throttling",
      "title": "Rate Limiting and Throttling",
      "meta_description": "Understand the difference between rate limiting, throttling, and spike control policies in API Manager, and how to configure each to protect backend systems.",
      "reading_time": "6 min read",
      "summary": "Rate limiting, throttling, and spike control are related but distinct API Manager policies that protect backend systems from excessive or bursty traffic.",
      "content_html": "\n<p>Backend systems &mdash; databases, legacy mainframes, third-party APIs &mdash; can only handle so much traffic before performance degrades or they fail outright. API Manager provides a family of traffic-management policies to protect them: <strong>rate limiting</strong>, <strong>throttling</strong>, and <strong>spike control</strong>. They sound similar but solve slightly different problems.</p>\n\n<h2>Rate Limiting</h2>\n<p>Rate limiting enforces a hard cap on the number of requests allowed within a defined time window. Once the limit is reached, subsequent requests are rejected immediately with an HTTP <code>429 Too Many Requests</code> response until the window resets.</p>\n<pre><code class=\"language-xml\">&lt;rate-limiting-policy&gt;\n  &lt;rate-limits&gt;\n    &lt;rate-limit timePeriodInMilliseconds=\"60000\" maximumRequests=\"1000\" /&gt;\n  &lt;/rate-limits&gt;\n&lt;/rate-limiting-policy&gt;</code></pre>\n<p>This configuration allows at most 1,000 requests per rolling 60-second window; the 1,001st request within that window is rejected outright rather than delayed.</p>\n\n<h2>Throttling</h2>\n<p>Throttling also caps the number of requests over a time period, but instead of immediately rejecting excess requests, it queues them and processes them once capacity frees up, up to a configured queue size and timeout. This smooths out bursts rather than dropping them, at the cost of added latency for queued requests.</p>\n<pre><code class=\"language-xml\">&lt;throttling-policy&gt;\n  &lt;throttling-limits&gt;\n    &lt;throttling-limit\n      timePeriodInMilliseconds=\"60000\"\n      maximumRequests=\"1000\"\n      queueTimeout=\"10000\" /&gt;\n  &lt;/throttling-limits&gt;\n&lt;/throttling-policy&gt;</code></pre>\n\n<h2>Rate Limiting vs Throttling</h2>\n<table>\n<thead><tr><th>Aspect</th><th>Rate Limiting</th><th>Throttling</th></tr></thead>\n<tbody>\n<tr><td>Excess requests</td><td>Rejected immediately (429)</td><td>Queued and delayed</td></tr>\n<tr><td>Client experience</td><td>Fails fast &mdash; client must retry</td><td>May succeed, just slower</td></tr>\n<tr><td>Best for</td><td>Enforcing hard usage caps per SLA tier</td><td>Smoothing bursty traffic to protect a fragile backend</td></tr>\n</tbody>\n</table>\n\n<h2>Spike Control</h2>\n<p>Spike control is different again: rather than a hard cap, it smooths out sudden traffic spikes over a short time window (measured in requests per second) to protect the backend from momentary bursts, regardless of the client's identity or SLA tier. It is commonly applied globally as a protective measure, independent of per-client rate limits.</p>\n<pre><code class=\"language-xml\">&lt;spike-control-policy&gt;\n  &lt;spike-control-limits&gt;\n    &lt;spike-control-limit maximumRequests=\"50\" timePeriodInMilliseconds=\"1000\" /&gt;\n  &lt;/spike-control-limits&gt;\n&lt;/spike-control-policy&gt;</code></pre>\n\n<h2>SLA-Based Rate Limiting</h2>\n<p>Rather than one fixed rate limit for every consumer, the <strong>SLA-based rate limiting</strong> policy ties different rate limits to different SLA tiers (e.g., Bronze: 100 req/min, Gold: 10,000 req/min), automatically applying the correct limit based on which tier the calling client's contract belongs to. This is the standard approach for public or partner-facing APIs with differentiated service levels.</p>\n\n<h2>Choosing the Right Policy</h2>\n<ul>\n<li>Use <strong>rate limiting</strong> when you need a strict, predictable ceiling and are comfortable rejecting excess traffic outright.</li>\n<li>Use <strong>throttling</strong> when occasional bursts are expected and acceptable latency is preferable to outright rejection.</li>\n<li>Use <strong>spike control</strong> as a general backend-protection safety net, independent of per-client business rules.</li>\n<li>Use <strong>SLA-based rate limiting</strong> whenever different consumer segments should get different levels of access.</li>\n</ul>\n\n<blockquote>Note: These policies can be combined &mdash; for example, spike control as a blanket protection layer plus SLA-based rate limiting for per-tier business rules on the same API instance.</blockquote>\n\n<h2>Key Points</h2>\n<ul>\n<li>Rate limiting rejects excess requests immediately; throttling queues and delays them instead.</li>\n<li>Spike control smooths short traffic bursts to protect the backend, independent of client identity.</li>\n<li>SLA-based rate limiting applies different limits automatically based on the consumer's approved tier.</li>\n<li>These policies are frequently layered together on the same API instance for defense in depth.</li>\n</ul>\n"
    },
    {
      "slug": "sla-tiers",
      "title": "SLA Tiers",
      "meta_description": "Learn how SLA tiers in API Manager define differentiated access levels for API consumers, and how contracts, approval, and rate limits work together.",
      "reading_time": "5 min read",
      "summary": "SLA tiers let API owners define multiple service levels, such as Gold, Silver, and Bronze, each with its own rate limits and approval rules.",
      "content_html": "\n<p>Not every consumer of your API should get the same level of access. A trusted internal system might need very high throughput, while a new external partner should start with conservative limits until trust is established. <strong>SLA tiers</strong> in API Manager let you define exactly that kind of differentiated access as a first-class, reusable concept.</p>\n\n<h2>What an SLA Tier Is</h2>\n<p>An SLA (Service Level Agreement) tier is a named policy bundle &mdash; most commonly a set of rate limits &mdash; that you define once on an API instance and then assign to individual client applications through <strong>contracts</strong>. A typical setup defines three or four tiers, for example:</p>\n\n<table>\n<thead><tr><th>Tier</th><th>Rate Limit</th><th>Approval</th><th>Typical Consumer</th></tr></thead>\n<tbody>\n<tr><td>Bronze</td><td>100 requests/min</td><td>Automatic</td><td>New or trial partners</td></tr>\n<tr><td>Silver</td><td>1,000 requests/min</td><td>Manual review</td><td>Established partners</td></tr>\n<tr><td>Gold</td><td>10,000 requests/min</td><td>Manual review</td><td>Strategic partners, internal systems</td></tr>\n</tbody>\n</table>\n\n<h2>Creating SLA Tiers</h2>\n<p>In API Manager, open the API instance, go to the <strong>SLA Tiers</strong> tab, and define each tier's name, description, and the rate limit that should apply. SLA tiers only take effect once you also apply an <strong>SLA-based rate limiting</strong> policy to the same API instance &mdash; the tier defines the numbers, the policy is what actually enforces them at runtime.</p>\n\n<h2>Auto-Approval vs Manual Approval</h2>\n<p>Each SLA tier can be configured as:</p>\n<ul>\n<li><strong>Auto-approved</strong> &mdash; any client application that requests this tier is granted a contract immediately, no human involved. Good for low-risk, low-limit tiers meant for broad, self-service access.</li>\n<li><strong>Manually approved</strong> &mdash; a request for the tier creates a pending contract that an API owner must explicitly approve in API Manager before credentials become active. Appropriate for higher-privilege tiers where you want a human decision in the loop.</li>\n</ul>\n\n<h2>Contracts</h2>\n<p>A <strong>contract</strong> is the link between a specific client application and a specific SLA tier on a specific API instance. When a developer finds an API in Anypoint Exchange and requests access, they choose which registered client application will consume it and which SLA tier they want; that request becomes a contract, and its status (pending, approved, revoked) governs whether requests from that client are allowed through.</p>\n\n<h2>Changing or Revoking Access</h2>\n<p>API owners can revoke a contract at any time (immediately cutting off that client, or after a grace period), or move a client to a different tier as the relationship evolves &mdash; for example, promoting a partner from Bronze to Silver once they prove reliable usage patterns, without requiring the client to change any code beyond their existing credentials continuing to work under the new limits.</p>\n\n<h2>Why SLA Tiers Matter</h2>\n<ul>\n<li>They let a single API implementation serve many different consumer relationships fairly, without hard-coding per-client logic into the flow.</li>\n<li>They give API product owners a business-friendly way to reason about access (\"who is on Gold?\") instead of raw technical rate-limit numbers scattered across configuration.</li>\n<li>They integrate directly with Anypoint Exchange's self-service API discovery and request flow, reducing the manual coordination needed to onboard new consumers.</li>\n</ul>\n\n<blockquote>Tip: Always keep at least one conservative, auto-approved tier (like Bronze) available so new or trial consumers can start integrating immediately without waiting on a manual approval cycle.</blockquote>\n\n<h2>Key Points</h2>\n<ul>\n<li>SLA tiers define named, reusable service levels (typically rate limits) for an API instance.</li>\n<li>They require an SLA-based rate limiting policy to actually be enforced.</li>\n<li>Tiers can be auto-approved or require manual approval before a contract becomes active.</li>\n<li>A contract links a specific client application to a specific tier and controls whether its requests are allowed.</li>\n</ul>\n"
    }
  ]
}